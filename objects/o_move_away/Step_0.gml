/// @description Insert description here


x -= 10000;
friend = instance_nearest(x+10000, y, o_move_away);
x += 10000;

direction = point_direction(friend.x,friend.y,x,y)+90
if point_distance(friend.x,friend.y,x,y) > 100
{
speed = 0.5
}
