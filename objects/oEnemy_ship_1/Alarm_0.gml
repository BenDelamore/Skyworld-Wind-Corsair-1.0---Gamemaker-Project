/// @description Chase Player Path Finding

/*
if mode = "chase"
{
	if (mp_grid_path(global.ai_grid, path, x, y, xx, yy, true))
	{
		path_start(path, spd, path_action_stop, false);
	}
}

alarm[0] = 1*room_speed